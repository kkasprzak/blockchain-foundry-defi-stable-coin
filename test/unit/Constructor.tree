DSCEngineConstructorTest
├── when token and price feed lists have different lengths
│   └── it should revert
└── when lists have equal lengths
    └── it should register each token with its price feed